# AI chat via OpenRouter (required for the "Talk to My Resume" assistant)
OPENROUTER_API_KEY=

# Optional: Upstash Redis for chat rate limiting (falls back to in-memory if unset)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
