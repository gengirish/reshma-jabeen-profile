# AI chat via NVIDIA NIM (preferred — OpenAI-compatible endpoint)
NIM_API_KEY=

# AI chat via OpenRouter (fallback if NIM_API_KEY is not set)
OPENROUTER_API_KEY=

# Optional: Upstash Redis for chat rate limiting (falls back to in-memory if unset)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
